To start server database use command
```sh
db/runHSQLDBServer.sh
```
next, run spring application to create empty tables in db <br> 
and after that use command
```sh
db/runHSQLDBClient.sh
```
to run database in window mode.
